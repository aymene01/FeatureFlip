{
  "name": "@feature-flip/feature-flag-platform",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "web:dev": "turbo dev --filter=web",
    "api:dev": "turbo dev --filter=feature-flag-platform-api",
    "lint": "turbo lint",
    "storybook": "turbo storybook",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
